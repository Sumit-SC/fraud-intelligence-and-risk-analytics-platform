# Streamlit Router (Shared)

Shared configuration and routing for the Streamlit apps.

## Contents
- `router.py` — routes users to Basic vs Advanced modes
- `check_streamlit_errors.py` — lightweight diagnostics
- `test_streamlit_pages.py` — basic page-load tests

## Usage
This folder is used by the root `streamlit_app.py` entry point.
