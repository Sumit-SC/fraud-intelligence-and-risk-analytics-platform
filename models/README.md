# Models

Persisted ML artifacts for risk scoring and explainability.

## Files
- `lr_model.pkl` — baseline model
- `rf_model.pkl` — production model
- `scaler.pkl` — feature scaling pipeline
- `model_metadata.pkl` — metadata and training context

## Regenerate
Model training is documented in `notebooks/README.md`.
